"use client";
import React, { useState } from "react";
import Link from "next/link";

interface DropdownProps {
  label: string;
  links: string[];
}

export const Dropdown: React.FC<DropdownProps> = ({ label, links }) => {
  return null; // This component is removed for now
};
